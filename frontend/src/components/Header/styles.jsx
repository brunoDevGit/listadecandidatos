import styled from 'styled-components';

export const Container = styled.div`
  
  header.header{
      background-color: #ffffff;
      padding: 0px 15px;
      overflow: hidden;
      white-space: nowrap;
      box-shadow: var(--shadow);
      box-shadow: 10px 25px 20px -4px #00CED1;
      
        
  }

  header.header h1{
    font-size: 1.8em;
  }

`;
